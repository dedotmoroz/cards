import { styled } from '@mui/material/styles';
import { AiContentIcon } from '@/shared/icons';

export const StyledAiContentIcon = styled(AiContentIcon)`
    margin-top: 3px;
    margin-bottom: 1px;
`;
