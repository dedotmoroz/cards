import type { CheckboxProps } from '@mui/material';
import { StyledCheckbox } from './styled-components';

export const CheckboxUI = (props: CheckboxProps) => {
    return <StyledCheckbox {...props} />;
};

