# 🤖 KotCat Telegram Bot

Telegram-бот для сервиса **KotCat** — изучение иностранных слов **в живом контексте**, а не списками.

Бот является **тонким клиентом**:
- не хранит бизнес-логику
- не хранит пользователей
- работает через backend API
- использует сервисную авторизацию

---

## 🚀 Возможности (MVP)

- `/start` — начало работы
- Проверка, привязан ли Telegram-аккаунт
- Подключение аккаунта через веб-приложение
- Загрузка папок пользователя
- Контекстное чтение слов:
    - текст
    - скрытый перевод (по кнопке)
    - переход к следующему фрагменту
- Stateless UI + session только для навигации

---

## 🧱 Архитектура

```telegram-bot/
├─ src/
│  ├─ index.ts              # entrypoint
│  ├─ bot.ts                # фабрика бота
│  ├─ config/
│  │  └─ env.ts             # env + проверки
│  ├─ api/
│  │  └─ client.ts          # HTTP-клиент к backend
│  ├─ handlers/
│  │  ├─ index.ts           # регистрация handlers
│  │  ├─ commands/
│  │  │  └─ start.ts
│  │  └─ callbacks/
│  │     ├─ folders.ts
│  │     ├─ next.ts
│  │     └─ translate.ts
│  └─ types/
├─ .env                     # переменные окружения
├─ package.json
├─ tsconfig.json
└─ README.md


### Ключевые принципы
- ❌ нет глобального `bot` в handlers
- ✅ dependency injection (`registerX(bot)`)
- ✅ один `Bot` → много handlers
- ✅ backend — единственный источник истины

---

## 🔐 Авторизация

### Типы авторизации
- **Telegram Bot → Backend**
    - `Authorization: Bearer BOT_SERVICE_JWT`
    - `X-Telegram-User-Id`
- **Пользователи**
    - обрабатываются **только backend**
    - бот userId не знает

---

## 🔧 Переменные окружения

Создай файл `.env` в корне проекта:

```env
TG_BOT_TOKEN=123456789:AA...
BOT_SERVICE_JWT=service_jwt_for_backend
API_URL=https://kotcat.com/api
WEB_APP_URL=https://kotcat.com