WORDS_TO_USE:
{WORDS_LIST}

TARGET_LANGUAGE: "{TARGET_LANGUAGE}"
TRANSLATION_LANGUAGE: "{TRANSLATION_LANGUAGE}"
CEFR_LEVEL: {CEFR_LEVEL}

Instructions:

1) Understanding WORDS_TO_USE:
   - Each item in WORDS_TO_USE contains:
     a) a word or phrase in TARGET_LANGUAGE (this is the form that MUST be used in the text)
     b) an optional explanation or translation in parentheses
   - Text inside parentheses is provided ONLY to clarify meaning.
   - Parentheses content MUST NOT appear in the generated text.

2) Languages:
   - Write the main text in TARGET_LANGUAGE.
   - Write the translation in TRANSLATION_LANGUAGE.
   - Do NOT infer or swap these languages.

3) Text Requirements:
   - Create ONE coherent, engaging text (story, article, or narrative).
   - The text must be a complete, flowing narrative — not a list of sentences.
   - All original words or phrases from WORDS_TO_USE must be used naturally.
   - Words may be grammatically transformed if appropriate.
   - Across the entire text, each word or phrase from WORDS_TO_USE should appear multiple times.
   - On average, each sentence should contain around 2 words from WORDS_TO_USE.
   - The text should be compact and concise, avoiding unnecessary details or filler.
   - Prefer shorter sentences and a limited number of sentences while keeping the text coherent.
   - Aim for the minimum length required to naturally incorporate all words.

4) Level:
   - The text complexity must strictly match CEFR_LEVEL.
   - Use natural, authentic language appropriate for this level.

5) Translation:
   - Provide a complete, natural translation of the entire text in TRANSLATION_LANGUAGE.
   - Translate meaning, not form.
   - Each word or phrase from WORDS_TO_USE must be translated according to its meaning from the parentheses.

6) Output Format:
   - Do NOT swap these fields.
   - Output ONLY valid JSON with this exact structure:

{
  "text": "Complete coherent text in TARGET_LANGUAGE",
  "translation": "Complete translation in TRANSLATION_LANGUAGE"
}

7) Self-Check and Regeneration:
   - After generating the text, internally verify that:
     a) ALL words or phrases from WORDS_TO_USE are used at least once
     b) The text reads naturally and is not overloaded with vocabulary
     c) The text matches the specified CEFR_LEVEL
   - If these conditions are not met, regenerate the text.
   - Do NOT mention the self-check in the output.
   - Verify that the "text" field is written in TARGET_LANGUAGE.
   - Verify that the "translation" field is written in TRANSLATION_LANGUAGE.
   - Verify that original words or phrases from WORDS_TO_USE appear ONLY in "text" and NEVER in "translation".
   - Verify that "text" and "translation" are NOT swapped.
   - If any of these checks fail, regenerate the entire output.

Important:
- Do NOT include explanations, comments, or markdown.
- Do NOT repeat WORDS_TO_USE in the output.
- Do NOT include text from parentheses in the original text.
- Return ONLY the JSON object.
